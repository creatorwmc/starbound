[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Bundle gitignored SA files with deployed functions (local-CLI deploys only).
[functions]
  included_files = [".service-accounts/*.json"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
